#include "cube.h"
#include <stdio.h>

void	draw_aim(t_game g)
{
//	int	i;
//	int	j;
	t_img	t;

	t = g.txt[6];
//	j = 0;
	printf("t.x : %d\tt.y : %d\n", t.x, t.y);
}
/*	while (j < g.set.res_y)
	{
		i = 0;
		while (i < g.set.res_x)
		{
			if (
*/
